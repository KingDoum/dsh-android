# DSH Android 客户端 — 架构文档

> **项目**：DSH（DeepSeek Harness）Android 原生客户端
> **仓库**：https://github.com/KingDoum/dsh-android （GHA 自动构建 APK）
> **技术栈**：Kotlin 1.9 + Jetpack Compose (BOM 2024.02) + Material 3 + OkHttp + kotlinx.serialization
> **版本**：1.2.0（versionCode 3）
> **最后更新**：2026-08-20

---

## 一、项目定位

DSH（DeepSeek Harness）是自部署的 AI Agent 平台（`https://dsh.113096.xyz:4443`）。本 APP 是其 **Android 原生客户端**，通过 DSH 的 RPC API + WebSocket 实时事件流直接通信，**不依赖 WebView**，提供原生会话管理 + 聊天体验。

---

## 二、总体架构

```
┌─────────────────────────────────────────────────────┐
│                    Android App                        │
│  ┌─────────────────────────────────────────────┐     │
│  │        UI Layer (Jetpack Compose + M3)      │     │
│  │  SessionListScreen │ ChatScreen │ Settings │     │
│  └─────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────┐     │
│  │      ViewModel Layer (StateFlow)             │     │
│  │  SessionListViewModel │ ChatViewModel        │     │
│  └─────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────┐     │
│  │             Data Layer (API)                 │     │
│  │  DshApi → DshRpcClient (HTTP POST /api/*)    │     │
│  │          → DshEventClient (WebSocket events) │     │
│  └─────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────┐     │
│  │         Domain Model (Models.kt)             │     │
│  │  SessionSummary │ Message │ ToolCall │ Rpc*  │     │
│  └─────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────┘
                        │
                        ▼
     ┌──────────────────────────────────────┐
     │           DSH Server                  │
     │  dsh.113096.xyz:4443                  │
     │  POST /api/session.list 等             │
     │  WS /api/events.mux 实时事件流         │
     └──────────────────────────────────────┘
```

---

## 三、包结构（17 个源文件，2382 行）

```
com.dsh.client/
├── DshApp.kt                    # Application 入口 + 单例 API 提供（@Volatile + synchronized）
├── MainActivity.kt              # 单 Activity，enableEdgeToEdge + DSHTheme
├── data/
│   ├── api/
│   │   ├── RpcModels.kt (434行)  # RPC 协议模型 + MuxFrame 密封类 + SessionEventParser（增强）
│   │   ├── DshRpcClient.kt      # HTTP RPC 客户端（inline reified + suspendCancellableCoroutine）
│   │   ├── DshEventClient.kt    # WebSocket 事件流（指数退避重连 + SharedFlow）
│   │   └── DshApi.kt            # 高层 API 封装（listSessions/createSession/getHistory/sendMessage...）
│   └── cache/
│       └── LocalCache.kt        # JSON 文件本地缓存（离线会话/消息缓存）
├── domain/model/
│   └── Models.kt                # SessionSummary / Message / MessageRole / ToolCall / AgentPreset
└── ui/
    ├── navigation/AppNavigation.kt  # 三 tab 底部导航 + NavHost
    ├── sessionlist/                 # 会话列表（左滑删除/置顶、长按菜单、预览）
    ├── chat/                        # 聊天（Markdown 渲染、流式打字机、工具卡片、复制菜单）
    ├── settings/                    # 设置页（服务器地址配置）
    └── theme/                       # Color.kt / Theme.kt / DSHSurfaceTheme.kt
```

---

## 四、DSH RPC 协议（实测验证）

### 4.1 请求/响应格式

```json
// 请求：POST /api/<method>
{"type":"client-request","rpcId":"uuid","method":"session.list","payload":{}}
// 响应
{"type":"server-response","rpcId":"uuid","result":{"ok":true,"value":{...}}}
// 错误
{"result":{"ok":false,"error":{"code":"xxx","message":"..."}}}
```

### 4.2 核心 API（已实测通过）

| 方法 | 用途 | 返回 value 结构 |
|------|------|----------------|
| `session.list` | 会话列表 | `{items:[{sessionId, updatedAt, running, blank, agentPreset, projections:{values:{title}}}]}` |
| `session.create` | 新建会话 | `{sessionId, agentPreset}` |
| `session.history` | 历史消息 | `{events:[{event:{type,seq,time,data}}], hasMore}` |
| `session.prompt` | 发送消息 | `{accepted}` |
| `session.cancel` | 停止生成 | `{}` |
| `session.rename` | 重命名 | `{title, seq}` |

### 4.3 事件类型（WebSocket）

| 事件类型 | 含义 | 数据处理位置 |
|---------|------|-------------|
| `user/message` | 用户消息 | `data.content` = blocks 数组 |
| `assistant/message` | AI 完整回复 | `data.message.content` = blocks 数组（**注意嵌套**） |
| `assistant/chunk` | 流式增量 | `data.chunk` |
| `session/title` | 标题变更 | `data.title` |
| `tool/call` / `tool/result` | 工具调用 | `data.callId` / `data.name` |
| `session/subscribed` | 订阅确认 | `lastSeq` |

### 4.4 关键陷阱（已踩过）

- **assistant/message 的 content 嵌套在 `data.message.content`**，user/message 直接在 `data.content` — `SessionEventParser` 已处理
- 会话标题在 `session.list` 的 `projections.values.title`，需要 extractTitle 提取
- WebSocket URL 是 `/api/events.mux`（在 RPC 客户端基础上加）
- `Call.await()` 需要自实现（用的是 suspendCancellableCoroutine，非 okhttp 内置）

---

## 五、数据流

### 发送消息
```
用户输入 → ChatViewModel.sendMessage()
  → 乐观消息（temp-前缀）立即显示
  → _pendingReplacements[content]=tempId（网络调用前记录！）
  → DshApi.sendMessage() → POST /api/session.prompt
  → WebSocket 推送 user/message → 按内容匹配替换乐观消息
  → assistant/chunk 增量 → isStreaming=true 更新气泡
  → assistant/message → 完整内容替换
```

### 加载历史
```
进入会话 → ChatViewModel.loadHistory()
  → DshApi.getHistory() → POST /api/session.history (maxMessages=50)
  → SessionEventParser.parse() 解析 events[] → Message 列表
```

### 会话列表
```
启动 → SessionListViewModel.loadSessions()
  → DshApi.listSessions() → SessionSummary（title 从 projections 提取）
  → WebSocket 事件节流刷新（1s 冷却）
```

---

## 六、关键技术决策

| 决策 | 选择 | 原因 |
|------|------|------|
| UI | Jetpack Compose | 声明式，适合聊天类复杂状态 |
| 网络 | OkHttp（非 Retrofit） | RPC 不是标准 REST |
| 序列化 | kotlinx.serialization | Kotlin 原生，配合 Compose |
| 架构 | 单 Activity + ViewModel + StateFlow | 简单清晰 |
| 实时 | WebSocket + SharedFlow | 低延迟，指数退避重连（1s→30s） |
| 单例 | DshApp companion + @Volatile + synchronized | 双重检查锁防重复创建 |

---

## 七、已知限制（S2 现状）

1. **无系统级代码高亮** — 代码块使用等宽字体 + 背景色，无语言关键词着色（可后续集成 highlight.js）
2. **LocalCache 非 Room** — 使用 JSON 文件缓存而非 Room 数据库（适合离线缓存，不适合复杂查询）
3. **无 Material You 动态取色** — 主题色固定（P2 待做）
4. **无大屏自适应** — 基于手机设计（P2 待做）
5. **无会话搜索** — 服务端支持 session.search RPC，客户端未实现 UI（P2 待做）
6. **无首次启动引导页** — 首次使用无引导（P2 待做）
7. **无嵌套 Markdown 样式** — 如 **bold *italic* text** 不会正确渲染

详见 `开发文档.md` 的 P2 改造路线。
